# OpenAI API key
OPENAI_API_KEY=sk-...

# Password for code-server (fallback method)
PASSWORD=changeme

# Domain used in Caddy config
DOMAIN=dev.gresky.yourdomain.com

# Email for Let's Encrypt TLS
EMAIL=you@example.com

# Basic Auth credentials for HTTPS login
BASIC_AUTH_USER=admin

# Generate using: `caddy hash-password`
BASIC_AUTH_PASS_HASH=$2a$14$exampleGeneratedSecureHash
